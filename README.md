# Melon-mp4

1. 開啟 index.html。
2. 選擇 MP4、MOV 或 WebM。
3. 設定解析度、FPS 與秒數。
4. 按「生成 .melsave」。

注意：
- 第一次開啟需要網路載入 JSZip。
- 影片轉換完全在瀏覽器本機進行，不會上傳。
- 此版本使用 mc mp4.melsave 作為地圖模板，保留 LED 矩陣與接線，只替換 Lua 影片內容。
- 建議先用 48×27、15 FPS、15 秒以內測試。
